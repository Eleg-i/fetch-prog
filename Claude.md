# Claude

## 范围

规则适用于 `fetch-prog` 项目。

## 配套文档

- 项目文件语义索引：[filesIndex.md](./filesIndex.md)
- 项目代码图谱索引：`codegraph`, `codebase-memory-mcp`
- 项目开发总纲、阶段统计与子计划索引：[开发计划.md](./开发计划.md)
- 测试用例：[测试用例.md](./测试用例.md)

## 规则

1. 本子项目必须维护 `开发计划.md` 和 `测试用例.md`，测试只写在本子项目内。
2. TDD 开发。
   - 测试框架统一使用 Vitest，配置入口为 `test/vitest.config.ts`，测试前置为 `test/setup.ts`。
   - 测试代码统一放在根目录 `test/` 文件夹，单元测试命名为 `test/unit/**/*.spec.ts`。
   - 常用命令：`pnpm run test` 执行测试，`pnpm run test:watch` 进入监听模式，`pnpm run test:coverage` 输出覆盖率。
   - 测试描述必须使用中文；测试辅助函数必须写 JSDoc；不得为测试降低 TypeScript、lint 或运行校验严格度。
   - WGSL 测试优先覆盖源码契约与 CPU 参考公式；真实 GPU readback 放到后续集成测试，不强塞进单元测试。
3. 每次开发对话必须先调「节省词元」skill。
4. 每次开发对话调「开发计划」skill。
5. 开发先调「开发规范索引」skill，再按指引调 ts 开发规范 skill。
6. 未明确要求，不改 `上下文交换.md`。
7. 别先指挥用户下一步；先看根 `开发计划.md` 的当前焦点，再进入对应阶段子计划查看「正在推进」和「近期优先级」。计划不合理可提修改意见。
8. 了解项目使用三层索引：先调用 `codegraph` 分析代码结构和框架；再调用 `codebase-memory-mcp` 做符号、调用关系、依赖和实现细节的精确检索。如果 MCP 没有索引，先自己调用 mcp 建立索引，再根据代码库内容回答。最后读取 `filesIndex.md` 获取人工理解过的项目语义地图，确定文档、模块、测试与实现入口。如果以上都没有充分的信息就直接搜索文件。
9. ts 的每个类型定义都要写注释，写清楚这个类似是什么，用来干什么。每个文件也要写 @file 的 jsdoc 注释。
10. 只要是 verify 暴露的问题都要在本次解决，不论是否相关，因为每次都这样就能保证下次不会有问题。

## 工作流

1. 参考 `工作流.md` 进行全局规划，对需求进行分析，制定开发计划。
2. 不要在工作流中更新 `filesIndex.md`。

## 计划

1. 如果是对“开发计划”中的下一个小目标进行计划，则要求严格按工作流的复杂问题，调用 `编码工作流` skill，严格按格式输出计划给用户审核。
2. 如果是其他简单计划，没用指明用哪种计划方式，则使用简单流程进行计划，直接在主 Agent 执行规划任务。

## 执行计划

1.严格按照计划的执行，一般需要调用 workflow，然后设计 workflow 并执行。

## 业务规则


