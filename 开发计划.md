# fetch-prog 单元测试开发计划

严格按公开 API 行为写测试；测试目录镜像 `src`；先验证运行时行为，再验证类型提示；每轮完成后更新本计划状态。

## 目录

- [本轮更新摘要](#本轮更新摘要)
- [最终目标](#最终目标)
- [技术方案](#技术方案)
- [测试目录约定](#测试目录约定)
- [阶段拆分](#阶段拆分)
- [已完成](#已完成)
- [进行中](#进行中)
- [待确认事项](#待确认事项)

## 本轮更新摘要

本轮完成子目标四：测试体系已完成收尾，`verify` 已纳入格式化、lint、类型检查和 `test:unit`，测试文件与 Vitest 配置也进入格式和 lint 校验范围。

## 最终目标

项目具备可持续维护的 Vitest 测试体系，并满足以下可验证标准：

| 标准 | 验收方式 |
|------|----------|
| 行为 API 按预期实现 | `pnpm run test:unit` 能执行运行时单元测试并通过 |
| 类型提示符合需求 | 类型测试能验证 `fetch<TRes>()`、`fetch<TRes, TData>()`、`Options`、`FetchError`、`ExtendableResponse` 等公开类型 |
| 测试结构可定位 | `test/` 目录结构与 `src/` 保持对应 |
| 工程校验不退化 | `pnpm run verify` 和 `pnpm run build` 继续通过 |
| 文档同步 | API 行为变化时同步更新 `API.md` 和本计划状态 |

## 技术方案

| 项目 | 方案 |
|------|------|
| 测试框架 | 使用 `vitest` |
| 测试环境 | 以 `jsdom` 或 `happy-dom` 作为 Web API 测试环境；若原生 Node 版本已满足 `fetch`、`ReadableStream`、`FormData`，优先减少环境依赖 |
| 测试命令 | 新增 `test:unit`，执行 Vitest 单元测试 |
| 类型测试 | 每个类型用例独立放在 `test/type-cases/`，由 Vitest spawn `tsc` 并以 shell 退出码和输出作为判断依据 |
| 测试风格 | 按 TDD 的纵向切片推进：一个行为用例、一轮实现或修正、一轮验证 |
| 测试边界 | 优先测试包公开 API，不测试私有方法；内部 utils 只测试被当前公开行为依赖且已有独立导出的工具函数 |

## 测试目录约定

测试文件统一放在 `test/` 下，结构与 `src/` 对应：

```text
test/
  index.test.ts
  index.type.test.ts
  type-cases/
    pass/
      *.ts
    fail/
      *.ts
  utils/
    object.test.ts
    url.test.ts
    bytes.test.ts
    formate.test.ts
  types/
    serializable.type.test.ts
```

命名约定：

| 文件类型 | 用途 |
|----------|------|
| `*.test.ts` | 运行时行为测试 |
| `*.type.test.ts` | 类型测试调度器，负责 spawn `tsc` 检查独立类型 case |
| `test/type-cases/pass/*.ts` | 预期 `tsc` 编译通过的类型用例 |
| `test/type-cases/fail/*.ts` | 预期 `tsc` 编译失败且输出包含指定内容的类型用例 |

## 阶段拆分

### 阶段一：引入 Vitest 基础设施

| 项目 | 内容 |
|------|------|
| 阶段目标 | 让项目具备可执行的单元测试命令 |
| 关键交付物 | `vitest` 依赖、测试配置、`test:unit` 脚本、最小 smoke test |
| 风险和依赖 | 初始实施时需要安装新 devDependency，并确认 `test:unit` 是否纳入 `verify` |
| 完成标准 | `pnpm run test:unit` 能执行并通过；`pnpm run verify` 不受影响 |

### 阶段二：覆盖公开 API 运行时行为

| 项目 | 内容 |
|------|------|
| 阶段目标 | 验证 `Fetch` 类的核心运行时行为与 API 文档一致 |
| 关键交付物 | `test/index.test.ts`，覆盖请求分发、URL 拼接、参数序列化、请求体序列化、拦截器、错误处理、超时/取消、上传进度基础行为 |
| 风险和依赖 | 需要稳定 mock `self.fetch`；涉及 Web 标准对象，测试环境必须支持 `Response`、`Headers`、`ReadableStream`、`FormData` |
| 完成标准 | 所有公开行为测试通过，且测试只通过公开 API 观察结果 |

建议优先测试的行为：

| 优先级 | 行为 |
|--------|------|
| P0 | `PATCH` 与 `POST`/`PUT` 一样走请求体 |
| P0 | `paramsSerializer` 在 GET 类请求中生效 |
| P0 | `FormData` 不主动设置 `content-type` |
| P0 | `guard()` 返回 id，`unGuard()` 移除后不影响后续 id |
| P0 | 普通对象 `data` 清理 `undefined` 时不修改调用方对象 |
| P1 | `baseURL`、`params`、GET 对象 `data` 的 URL 生成 |
| P1 | 非 2xx 响应抛出或交给错误处理器 |
| P1 | 请求拦截器可修改配置，响应拦截器可转换返回值 |
| P2 | 上传 `ReadableStream` 时触发 `onUploadProgress` |

### 阶段三：覆盖 utils 行为

| 项目 | 内容 |
|------|------|
| 阶段目标 | 对公开行为依赖较强的工具函数补充低成本测试 |
| 关键交付物 | `test/utils/object.test.ts`、`test/utils/url.test.ts`、必要时补 `bytes` 和 `formate` 测试 |
| 风险和依赖 | utils 不是包级公开 API，测试不要绑定过多内部实现；只覆盖稳定、低层、容易回归的行为 |
| 完成标准 | `normalizeData`、`treeShake`、`joinPath`、`isSameOrigin` 等关键工具行为有基础保护 |

### 阶段四：覆盖 TypeScript 类型提示

| 项目 | 内容 |
|------|------|
| 阶段目标 | 验证用户使用包时的类型提示和类型约束符合预期 |
| 关键交付物 | `test/index.type.test.ts`、`test/type-cases/pass/*.ts`、`test/type-cases/fail/*.ts` |
| 风险和依赖 | 负向类型测试必须依赖真实 `tsc` 结果；`test/type-cases/` 需要从根 `tsconfig` 排除，避免故意失败的 case 破坏普通 `type-check` |
| 完成标准 | 响应泛型、请求体泛型、非法 `data`、`Options` 字段、`guard()` 返回 id、`FetchError` 属性等类型用例全部通过 |

建议优先测试的类型行为：

| 优先级 | 类型行为 |
|--------|----------|
| P0 | `fetch()` 默认返回 `Promise<ExtendableResponse>` |
| P0 | `fetch<TRes>()` 返回 `Promise<TRes>` |
| P0 | `fetch<TRes, TData>()` 要求 `data` 匹配 `TData` 且可序列化 |
| P0 | `symbol`、`function`、`bigint`、`any` 等非法请求体被拒绝 |
| P0 | `guard()` 返回 `{ request?: number; response?: number }` |
| P1 | `Options.method`、`cache`、`redirect`、`referrerPolicy` 只接受声明的联合类型 |
| P1 | `ExtendableResponse<E>` 能表达响应拦截器扩展字段 |

### 阶段五：收尾与持续维护

| 项目 | 内容 |
|------|------|
| 阶段目标 | 确保测试、类型、构建、文档形成闭环 |
| 关键交付物 | 通过的测试命令、更新后的 `API.md`、更新后的本计划状态 |
| 风险和依赖 | 若测试揭示 API 行为与文档不一致，需要先判断是修实现还是修文档 |
| 完成标准 | `pnpm run test:unit`、`pnpm run verify`、`pnpm run build` 全部通过 |

## 已完成

### 子目标一：引入 Vitest 基础设施

| 项目 | 内容 |
|------|------|
| 完成内容 | 已安装 `vitest`，新增 `pnpm run test:unit`，创建 `vitest.config.ts` 和 `test/index.test.ts` |
| 执行细节/技术方案 | 使用 Vitest 默认 Node 环境，不额外引入 `jsdom` 或 `happy-dom`；测试目录从 `test/index.test.ts` 开始，后续继续按 `src` 镜像扩展 |
| 结果或结论 | `pnpm run test:unit`、`pnpm run verify`、`pnpm run build` 均已通过；由于沙箱读取 `node_modules/.pnpm` 受限，验证命令使用提升权限执行 |

### 子目标二：API 行为测试用例落地

| 项目 | 内容 |
|------|------|
| 完成内容 | 已扩展 `test/index.test.ts`，覆盖计划中的 P0 API 行为 |
| 执行细节/技术方案 | 使用 mock `fetch` 观察公开 `Fetch` 实例发出的 `url` 与 `RequestInit`；通过用户可观察行为验证，不直接测试私有方法 |
| 结果或结论 | `PATCH` 带请求体、GET `paramsSerializer`、`FormData` 不设 `content-type`、`guard/unGuard` 稳定移除、对象 `data` 不被原地修改均已被测试保护；`pnpm run test:unit`、`pnpm run verify`、`pnpm run build` 均通过 |

### 子目标三：类型提示测试用例落地

| 项目 | 内容 |
|------|------|
| 完成内容 | 已新增 `test/index.type.test.ts`，覆盖计划中的 P0 类型行为和部分 P1 类型行为 |
| 执行细节/技术方案 | `test/index.type.test.ts` 作为调度器，逐个 spawn `tsc` 编译 `test/type-cases/pass` 和 `test/type-cases/fail` 中的独立文件；断言通过用例退出码为 `0`，失败用例退出码非 `0` 且输出包含预期片段 |
| 结果或结论 | `fetch()` 默认响应、响应泛型、双泛型请求体契约、非法 `symbol/function/bigint/any` 请求体、`guard()` 返回 id、`Options` 联合类型、`ExtendableResponse<E>` 扩展字段均已被独立 `tsc` case 覆盖；`pnpm run type-check`、`pnpm run test:unit`、`pnpm run verify`、`pnpm run build` 均通过 |

### 子目标四：收尾与持续维护

| 项目 | 内容 |
|------|------|
| 完成内容 | 已把 `test:unit` 纳入 `verify`，并把 `test/` 与 `vitest.config.ts` 纳入 Prettier 和 ESLint 校验 |
| 执行细节/技术方案 | 更新 `package.json` 脚本：`prettier` 覆盖 `src test vitest.config.ts`，`lint` 覆盖 `src test vitest.config.ts`，`verify` 串联 `type-check`、`prettier`、`lint`、`test:unit` |
| 结果或结论 | `pnpm run verify` 已完整通过，包含 2 个测试文件、13 个用例；`pnpm run build` 已通过；测试体系进入可持续维护状态 |

## 进行中

暂无进行中开发项。后续可继续补充 P1/P2 行为测试，或根据发布要求完善覆盖率、CI 与类型测试拆分命令。

## 待确认事项

| 事项 | 当前建议 |
|------|----------|
| 测试环境选择 | 当前 Node 环境已满足现有测试；若后续增加 DOM 专项行为，再评估 `happy-dom` 或 `jsdom` |
| 类型测试命令 | 当前类型测试由 `test:unit` spawn `tsc` 逐个 case 验证，并由 `verify` 闭环；如未来需要独立类型测试报告，再拆分专门命令 |
